import React, { Component, Fragment } from 'react';
import Svg from 'components/Svg';

const Spinner = (props) => (
    <Svg name="spin" height={30} width={30} {...props} />
);

export default Spinner;
